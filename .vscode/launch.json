{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Xdebug",
            "request": "launch",
            "type": "php",
            "hostname": "api.nomina.local.com",
            "port": 9003,
            "pathMappings": {
                "/var/www/back-kivo": "${workspaceFolder}"
            }
        },
    ]
}